# Server related settings
address: 0.0.0.0
port: 80
auth: true
tls: false
prefix: /
debug: false

# Default user settings (will be merged)
scope: /data
modify: true
rules: []

# CORS configuration
cors:
  enabled: true
  credentials: true

users:
  - username: ""
    password: ""
